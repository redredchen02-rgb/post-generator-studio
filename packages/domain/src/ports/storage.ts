import type { Generation, GenerationPreset, GenerationPresetCreate, GenerationPresetUpdate, GenerationStatus, PromptTemplate, PromptTemplateCreate, PromptTemplateUpdate, ProviderProfile, ProviderProfileCreate, ProviderProfileUpdate } from "../schemas.js";
export type GenerationCreateInput = { id: string; idempotencyKey?: string; title: string; eventSummary: string; providerProfileSnapshot: Record<string, unknown>; promptTemplateSnapshot: Record<string, unknown>; generationPresetSnapshot: Record<string, unknown>; renderedSystemPrompt: string; renderedUserPrompt: string; model?: string; providerKind?: ProviderProfile["providerKind"]; };
export type GenerationUpdateInput = Partial<{ outputContent: string; status: GenerationStatus; errorMessage: string; model: string; inputTokens: number; outputTokens: number; totalTokens: number; startedAt: string; completedAt: string; }>;
export interface ProviderProfileRepository { list(): Promise<ProviderProfile[]>; get(id: string): Promise<ProviderProfile | null>; create(input: ProviderProfileCreate & { id: string; apiKeyRef?: string; keyMasked?: string }): Promise<ProviderProfile>; update(id: string, input: ProviderProfileUpdate & { apiKeyRef?: string; keyMasked?: string | null }): Promise<ProviderProfile>; delete(id: string): Promise<void>; }
export interface PromptTemplateRepository { list(): Promise<PromptTemplate[]>; get(id: string): Promise<PromptTemplate | null>; create(input: PromptTemplateCreate & { id: string }): Promise<PromptTemplate>; update(id: string, input: PromptTemplateUpdate): Promise<PromptTemplate>; delete(id: string): Promise<void>; }
export interface GenerationPresetRepository { list(): Promise<GenerationPreset[]>; get(id: string): Promise<GenerationPreset | null>; create(input: GenerationPresetCreate & { id: string }): Promise<GenerationPreset>; update(id: string, input: GenerationPresetUpdate): Promise<GenerationPreset>; delete(id: string): Promise<void>; }
export interface GenerationRepository { list(opts: { limit: number; offset: number; search?: string }): Promise<{ items: Generation[]; total: number }>; get(id: string): Promise<Generation | null>; getByIdempotencyKey(key: string): Promise<Generation | null>; create(input: GenerationCreateInput): Promise<Generation>; update(id: string, input: GenerationUpdateInput): Promise<Generation>; delete(id: string): Promise<void>; }
export interface StoragePort { providerProfiles: ProviderProfileRepository; promptTemplates: PromptTemplateRepository; generationPresets: GenerationPresetRepository; generations: GenerationRepository; }
